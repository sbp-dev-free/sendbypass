export const metadata: Metadata = {
  title: "Sendbypass | Newsletter",
};
import { Metadata } from "next";

import { Newsletter } from "@/components/pages";
export default function NewsletterPage() {
  return <Newsletter />;
}
