import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Sendbypass | Referral Page",
};
import { Referral } from "@/components/pages";
export default function ReferralPage() {
  return <Referral />;
}
